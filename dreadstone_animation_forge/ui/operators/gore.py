"""Task-UI ownership marker for gore commands kept by the compatibility facade.

The public gore operator implementations remain in ``deformation_authoring`` so
old scripts and .blend buttons retain their exact identifiers.  This module is
the deliberate UI-layer home for future additive orchestration only.
"""

CLASSES = ()
